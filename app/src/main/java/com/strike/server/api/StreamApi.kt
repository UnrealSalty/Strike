package com.strike.server.api

import com.strike.server.Response

/** Live camera feed for the dashboard, in car and on the phone. */
object StreamApi {

    fun open(camera: Int): Response = TODO()

    fun snapshot(camera: Int): Response = TODO()
}
